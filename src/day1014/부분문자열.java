package day1014;

import java.util.LinkedList;
import java.util.Queue;

public class 부분문자열 {
    public int solution(String t, String p) {
        char[] tArr = t.toCharArray();
        Queue<Character> myQueue = new LinkedList<>();
        for(int i = 0;i<tArr.length-p.length()+1;i++){

        }
        return 0;
    }
}
